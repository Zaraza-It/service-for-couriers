spring:
  application:
    name:
      auth-service

  datasource:
    url:
      jdbc:postgresql://localhost:5432/user_db
    hikari:
      username: postgres
      password: garage123
      driver-class-name: org.postgresql.Driver
      auto-commit: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


